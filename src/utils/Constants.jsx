const INDEX_PER_PIXEL = 4;
const defaultCycleAmt = 25;
const defaultXNodeSize = 400;
const defaultYNodeSize = 400;
const canvasWidth = 200; 
const canvasHeight = 200;

export const Constants = {
    INDEX_PER_PIXEL,
}
